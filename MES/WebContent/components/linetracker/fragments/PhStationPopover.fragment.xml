<core:FragmentDefinition xmlns="sap.m"
	xmlns:core="sap.ui.core">
	<Popover id="sapContentPopUpLinetracker" class="sapUiContentPadding"
		placement="VerticalPreferredBottom">
		<VBox>
			<Button id="stationTracker" text="{i18n>stationTracker}" type="Transparent" press="gotoStationTracker" />
			<!-- <Button text="{i18n>stationKPIHeader}" type="Transparent"
				press="displayStationKPIHeader" /> -->
			<Button id="disruptionAndonTracker" text="{i18n>disruptionAndonTracker}" type="Transparent"
				press="gotoDisruptionAndonTracker" />
			<Button id="stationHandover" text="{i18n>stationHandover}" type="Transparent" press="gotoStationHandover" />
		</VBox>
		<footer>
			<!-- <Toolbar>
				<core:Icon src="sap-icon://alert" class="sapUiSmallMarginBegin" />
				<Button id="reportDisruption" text="{i18n>reportDisruption}"
					press="handleEmailPress" />
			</Toolbar> -->
		</footer>
	</Popover>
</core:FragmentDefinition>
